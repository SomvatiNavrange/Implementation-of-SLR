# Implementation-of-SLR
Simple Linear Regression (SLR) is a supervised learning algorithm used to model the relationship between a dependent variable (Y) and a single independent variable (X). 
The main goal is to find the best-fitting straight line (regression line) that can predict Y from X.
